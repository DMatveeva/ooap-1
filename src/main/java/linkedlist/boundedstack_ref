Получились следующие расхождения с эталоном:

1. В конструкторе по умолчанию описала постусловие как "создан новый пустой стек с максимальным размером 32", но в
эталоне нет конструктора по умолчанию.

2. В конструкторе с параметром указала предусловие, "аргумент maxSize > 0", но в эталоне такого предусловия нет.
