1. Команды head, tail, right должны иметь постусловия о том, куда перемещен курсор, а в моем АТД их нет. Поначалу я
подумала, что эти методы - это
запросы, т.к. не приняла во внимание, что изменение положения курсора - это изменение состояния объекта. Потом я
все-таки осознала и переместила head, tail, right в раздел команд, но не прописала постусловия по невнимательности.

2.Запросы is_head, is_tail не имеют в эталоне предусловия, что список не пустой. Возможно, если список даже и пустой, то
 вернется просто false, и никакой неопределенности не будет.

3. Команды  add_tail, remove_all, right не имеют предусловия, что список пуст. Возможно, тут тоже нет неопределенности.
Если add_tail вызывается на пустом списке, то будет добавлен единственный элемент, который будет и головой и хвостом.
remove_all и right на пустом списке просто ничего не сделают.

4. В конструкторе - в моем АТД написано, что создан просто новый список, а надо указать, что он пустой.